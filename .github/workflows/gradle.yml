name: CI/CD github Actions & Docker (Local Server)

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  CI-CD:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Gradle Caching
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: make application.yml
        run: |
          cd ./src/main/resources
          touch ./application.yml
          echo "${{ secrets.YML }}" > ./application.yml

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew clean build -x test

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Docker build & push
        run: |
          docker build -f Dockerfile -t ${{ secrets.DOCKER_REPO }}/whispy_be .
          docker push ${{ secrets.DOCKER_REPO }}/whispy_be

      - name: Deploy to home server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOME_HOST }}       
          username: ${{ secrets.HOME_USER }}
          password: ${{ secrets.HOME_PASSWORD }}
          key: ${{ secrets.HOME_SSH_KEY }}
          port: 2223
          script: |
            docker rm -f whispy-springboot || true          # [수정] 기존 앱 컨테이너만 삭제 (없으면 무시)
            docker pull ${{ secrets.DOCKER_REPO }}/whispy_be
            docker run -d --name whispy-springboot -p 8880:8080 ${{ secrets.DOCKER_REPO }}/whispy_be   # [수정] 컨테이너 이름 명시 및 포트 매핑
            docker image prune -f
